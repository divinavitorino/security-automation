#Para las primeras pruebas de acceso, se sugiere utilizar la conexión HTTP. Una vez completada y validada la configuración del entorno, implemente la conexión a través de HTTPS
all:
  hosts:
    ad_server:
      ansible_host: 10.0.0.5  
      ansible_user: ansible  
      ansible_password: Serverserver@03  #Para evitar dejar la contraseña expuesta en el código, se sugiere utilizar Ansible Vault para almacenar las credenciales y anotarlas.
      ansible_connection: winrm  
      ansible_port: 5985
      ansible_winrm_transport: kerberos  
      ansible_winrm_server_cert_validation: ignore
